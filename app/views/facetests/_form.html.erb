<%= form_with(model: facetest) do |form| %>
  <% if facetest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facetest.errors.count, "error") %> prohibited this facetest from being saved:</h2>

      <ul>
        <% facetest.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :face_url %>
    <%= form.file_field :face_url, :multiple => true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
